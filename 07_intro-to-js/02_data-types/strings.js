// Write 3 examples of expressions. One using + (concatenation), another using [] (indexing), and another using .length. 
// Print out the result of your expressions using console.log.

let user = "John";
console.log("Hello " + user);

console.log(user[0]);

console.log(user.length);